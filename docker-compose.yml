version: '3.8'

services:
  backend:
    container_name: backend
    build:
      context: .
      dockerfile: backend/Dockerfile.dev
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./backend/:/app/
      - /app/node_modules
    env_file:
      - .env

